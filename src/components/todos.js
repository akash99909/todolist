import React,{ Components } from 'react'

class todos extends Components {
    render(){
        return (
            <h1>Todo List</h1>
        );
      }
    }
  

export default todos;
